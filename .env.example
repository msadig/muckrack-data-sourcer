# Multilogin Credentials
# Sign up at https://multilogin.com to get your credentials

# Your Multilogin account email
MULTILOGIN_EMAIL=your-email@example.com

# Your Multilogin account password
MULTILOGIN_PASSWORD=your-password

# Multilogin folder ID (get from Multilogin dashboard)
FOLDER_ID=your-folder-id

# Multilogin profile ID (get from Multilogin dashboard)
PROFILE_ID=your-profile-id

# Optional: Run browser in headless mode (true/false)
# Note: Headless mode requires Solo, Team, or Custom plan (not available on Starter)
HEADLESS=false

# Docker Configuration
# Set to 'true' to use local Docker Multilogin setup instead of cloud
# When true, connects to localhost:35000 (API) and localhost:45001 (automation)
# When false, uses cloud endpoints (api.multilogin.com and launcher.mlx.yt)
USE_LOCAL_DOCKER=false

# VNC Password for Docker setup (for accessing browser via noVNC at http://localhost:6080)
VNC_PASSWORD=vncpass
